# Python
__pycache__/
*.pyc

# macOS 元数据（曾把 data/detail/.DS_Store 带进待提交列表）
.DS_Store

# Local-only auth (use GitHub Secret in CI)
rank_auth.json
_auth_runtime.json

# Local previews
_pages_preview/

# WorkBuddy 会话工作区（记忆等内部产物，不随仓库发布）
.workbuddy/

# Per-snapshot xlsx that core writes alongside JSON; we strip them in
# ci_scrape.py too, but ignore here as a second line of defense.
data/daily/rank_*.xlsx
data/daily/rank_*.json

# refill.py（一键补档）的本地运行状态：记录上次备了哪几批、目标是谁。
# 纯本机中间产物，不进仓库 —— daily.yml 会 `git add data/`，不 ignore 会把它带上去。
data/detail/_refill_state.json
